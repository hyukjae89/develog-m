(function() {
    'use strict';

    /*
    * TODO: 필수값 확인
    */
    function checkRequiredValue(value) {
        if (value === undefined || value === null || value === "") {
            return false;
        }

        return true;
    };

    /*
    * TODO: 영문 확인
    */
    function checkOnlyEnglishValue(value) {

    };

    /*
    * TODO: 최소 입력 제한 확인
    */
    function checkMinimumInputValue(value, min) {

    }

})();